!Flow
version: '1'
with:
  read_only: true
  port_expose: $JINA_PORT
pods:
  - name: encoder
    uses: pods/query-encode.yml
    timeout_ready: 60000
    read_only: true
  - name: indexer
    uses: pods/query-index.yml
  - name: ranker
    uses: pods/res-rank.yml
