!Flow
version: '1'
executors:
  - name: du_encoder
    uses: DualEncoder
    py_module:
        - executors.py
    uses_with:
        model: v1_marco_de
        use_cuda: False
        device_id: 5
        batch_size: 32
  - name: vec_indexer
    uses: jinahub://SimpleIndexer/latest
    uses_metas:
        workspace: './workspace'
